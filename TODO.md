TODO

- Add MCP server (stdio) with tools for list_licenses, validate_existing, write_license, render_license.
- Add wiring instructions for MCP in docs/ (agent runtime configuration).
- Update README.md with new flags: --validate, --dry-run, --stdout, non-interactive JSON behavior.
- Actually finish validate, and make it make senses for customised licenses
